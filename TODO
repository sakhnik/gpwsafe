* Error handling, granular exceptions
* Detect where a password was actually pasted
* Secure memory allocation error handling (setuid root)
* Implement very strong random data generation

* Script to remove excessive headers
